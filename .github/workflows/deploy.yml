name: Deploy

on:
  push:
    branches: [develop, staging, master]
  workflow_dispatch:
    inputs:
      environment:
        description: 'デプロイ先の環境'
        required: true
        type: choice
        options:
          - develop
          - staging
          - production

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: ./.github/workflows/test.yml

  deploy:
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DEPLOY_ENV: >-
        ${{ github.event.inputs.environment
          || (github.ref_name == 'master' && 'production')
          || github.ref_name }}
    environment:
      name: >-
        ${{ github.event.inputs.environment
          || (github.ref_name == 'master' && 'production')
          || github.ref_name }}
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.3.8'
      - uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: bun-${{ hashFiles('bun.lock') }}
          restore-keys: bun-

      - run: bun install --frozen-lockfile
      - run: bun run db:migrate:deploy
        env:
          DIRECT_DATABASE_URL: ${{ secrets.DIRECT_DATABASE_URL }}
      - run: bun run db:generate:sql
        env:
          DIRECT_DATABASE_URL: ${{ secrets.DIRECT_DATABASE_URL }}
      - run: bun run build

      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          environment: ${{ env.DEPLOY_ENV }}

      - name: Upload secrets
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GOOGLE_GENERATIVE_AI_API_KEY: ${{ secrets.GOOGLE_GENERATIVE_AI_API_KEY }}
        run: |
          printf '%s' "$DATABASE_URL" | bunx wrangler secret put DATABASE_URL --env "$DEPLOY_ENV"
          printf '%s' "$GOOGLE_GENERATIVE_AI_API_KEY" | bunx wrangler secret put GOOGLE_GENERATIVE_AI_API_KEY --env "$DEPLOY_ENV"
